<%= label_tag :category, "Category " + @index %>
<%= select_tag "category " + @index, options_from_collection_for_select(@categories, :id, :name),
  name: "categories[category #{@index}]", include_blank: true, class: "form-control" %>
