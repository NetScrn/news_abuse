<h2><%= @category.name %></h2>

<%= link_to t(:new_article), new_article_category_path, class: "btn btn-primary pull-right"%>

<section class="articles">
  <% @category.articles.each do |article| %>
    <%= render "articles/article", article: article %>
  <% end %>
</section>

<% # react_component "Articles", { data: @category.articles } %>
