<%= simple_form_for [article, comment] do |f| %>
  <%= f.input :content, label: t("activerecord.attributes.comment.content") %>

  <%= f.submit t(:create_comment), class: "btn btn-success btn-sm" %>
<% end %>
