<header>
  <nav class="navbar navbar-default navbar-inverse " id="header">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        </a>
        <%= link_to root_path, class: "navbar-brand" do %>
          <h1>ews Abuse
            <%= image_tag "new_abuse.png", alt: "N", width: 27, heihgt: 27, class: "logo-img pull-left" %>
          </h1>
        <% end %>
        </a>
      </div>

      <div class="collapse navbar-collapse" id="navbar-collapse">
        <ul class="nav navbar-nav">
          <li><%= link_to t(:categories), categories_path %></li>
          <% if user_signed_in? && current_user.admin? %>
            <li><%= link_to "Admin", admin_root_path %></li>
          <% end %>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= current_user.username %>
                <%= image_tag "avatar1.png", alt: "avatar", width: 35, heigth: 35, class: "pull-right user-img" %></a>
              <ul class="dropdown-menu">
                <li><%= link_to t(:settings), edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to t(:logout), destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% end %>

          <% unless user_signed_in? %>
            <% unless current_page? "/" %>
            <li><%= link_to t(:registration), new_user_registration_path %></li>
            <% end %>
            <li><%= link_to t(:login), new_user_session_path %></li>
          <% end %>

        </ul>
      </div><!-- /.navbar-collapse -->

    </div><!-- /.container-fluid -->
  </nav>
</header>
